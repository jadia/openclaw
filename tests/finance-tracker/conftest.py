import sys
import os

# Add the skills directory to the python path so that we can import the tracker module
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '../../skills/finance-tracker')))
